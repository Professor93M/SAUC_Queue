import React from "react";

export default function ApplicationLogo({ classes }) {
    return (
        <div className={classes}>
            <img src="./logo.png" className="object-fill" alt="logo" />
        </div>
    );
}
